[project]
name = "ai_screening_tool_api"
version = "0.1.0"
dependencies = [
    "fastapi[standard]",
    "openai",
    "PyPDF2",
    "python-docx",
    "python-dotenv",
    "sqlalchemy",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
